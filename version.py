# Semantic Versioning (https://semver.org): MAJOR.MINOR.PATCH
# Bump this, commit, then tag the commit "vX.Y.Z" and push the tag to
# trigger a release build (see .github/workflows/release.yml).
__version__ = "1.0.0"
